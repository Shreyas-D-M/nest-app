/** Identity reported by the health endpoints. */
export const SERVICE_NAME = 'nest-api';

/**
 * Service version.
 *
 * A literal for now. It should be replaced with real build metadata (git SHA or
 * release tag) when continuous deployment is wired up, so that a running
 * instance can be traced back to a commit.
 */
export const SERVICE_VERSION = '0.0.0';
