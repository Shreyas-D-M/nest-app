{
  "name": "@nest/config",
  "version": "0.0.0",
  "private": true,
  "description": "Typed, fail-fast environment configuration loading",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@nest/types": "workspace:*",
    "@nest/validation": "workspace:*",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@nest/eslint-config": "workspace:*",
    "@nest/tsconfig": "workspace:*",
    "@types/node": "26.2.0",
    "eslint": "10.9.0",
    "typescript": "6.0.3",
    "vitest": "4.1.11"
  }
}
